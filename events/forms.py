from django import forms

# class EventForm(forms.Form):
#     name=forms.CharField(label='Username/Email',max_length=50)
#     password=forms.CharField(label='Password',widget=forms.PasswordInput, max_length = 50)